'use client';
import {useEffect,useMemo,useState} from 'react';
import {supabase} from '../lib/supabase';

const baseMenu=[['dashboard','Dashboard'],['stockin','Input Stok Gudang'],['warehouse','Stok Gudang'],['transfer','Transfer'],['opname','Input Sisa'],['history','Riwayat'],['products','Master Produk'],['users','User / Role']];
const canRevise=(role)=>['admin','store_leader','team_leader'].includes(role);
const fmt=(n)=>Number(n||0).toLocaleString('id-ID',{maximumFractionDigits:3});
const UNIT_OPTIONS=['gr','kg','pcs'];
const baseUnit=(p)=>p?.base_unit||p?.unit||'pcs';
const areaConfig=(p,area)=>area==='kasir'
  ? {unit:p?.kasir_unit||baseUnit(p),perBase:Number(p?.kasir_per_base||1)}
  : {unit:p?.kitchen_unit||baseUnit(p),perBase:Number(p?.kitchen_per_base||1)};
const areaToBase=(qty,p,area)=>Number(qty||0)/Math.max(areaConfig(p,area).perBase,0.000000001);
const baseToArea=(qty,p,area)=>Number(qty||0)*Math.max(areaConfig(p,area).perBase,0.000000001);

export default function Home(){
 const [session,setSession]=useState(null),[profile,setProfile]=useState(null),[tab,setTab]=useState('dashboard'),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[busy,setBusy]=useState(false),[msg,setMsg]=useState('');
 useEffect(()=>{let mounted=true; supabase.auth.getSession().then(({data})=>mounted&&setSession(data.session)); const {data}=supabase.auth.onAuthStateChange((_e,s)=>setSession(s)); return()=>{mounted=false;data.subscription.unsubscribe()};},[]);
 useEffect(()=>{if(!session){setProfile(null);return} loadProfile();},[session]);
 async function loadProfile(){const {data,error}=await supabase.from('profiles').select('full_name,role,store_id,stores(name,code)').eq('id',session.user.id).single(); if(error)setMsg(error.message); else {setProfile(data);setTab(data.role==='crew'?'crew-stock':'dashboard')}}
 async function login(e){e.preventDefault();setBusy(true);setMsg('');const {error}=await supabase.auth.signInWithPassword({email,password});if(error)setMsg(error.message);setBusy(false)}
 async function logout(){await supabase.auth.signOut();setTab('dashboard')}
 if(!session)return <Login email={email} password={password} setEmail={setEmail} setPassword={setPassword} login={login} busy={busy} msg={msg}/>;
 const storeId=profile?.store_id;
 const isCrew=profile?.role==='crew';
 const crewMenu=[['crew-stock','Stok'],['transfer','Transfer'],['crew-opname','Input Sisa'],['crew-history','Riwayat Saya']];
 const leaderMenu=[...baseMenu.slice(0,3),...(canRevise(profile?.role)?[['revise','Revisi Stok Gudang'],['revise-operational','Revisi Stok Operasional'],['revise-daily','Revisi Stok Awal/Akhir'],['revise-transfer','Revisi Transfer']]:[]),...baseMenu.slice(3)];
 return <div className={'shell '+(isCrew?'crew-shell':'')}><aside><div className="sidebrand"><b>RCM</b><span>Management Stock</span></div><nav>{(isCrew?crewMenu:leaderMenu).map(([id,label])=><button key={id} className={tab===id?'active':''} onClick={()=>setTab(id)}>{label}</button>)}</nav><button className="logout" onClick={logout}>Keluar</button></aside><main className="content"><header><div><small>STORE</small><h2>{profile?.stores?.name||'LC Rancamanyar'}</h2></div><div className="user">{profile?.full_name||session.user.email}<small>{profile?.role||'crew'}</small></div></header>{msg&&<div className="error top-error">{msg}</div>}<section>
 {isCrew&&tab==='crew-stock'&&<CrewStock storeId={storeId}/>} 
 {isCrew&&tab==='transfer'&&<Transfer storeId={storeId}/>} 
 {isCrew&&tab==='crew-opname'&&<CrewOpname storeId={storeId}/>} 
 {isCrew&&tab==='crew-history'&&<MyHistory storeId={storeId}/>} 
 {!isCrew&&tab==='dashboard'&&<Dashboard storeId={storeId} setTab={setTab}/>} 
 {!isCrew&&tab==='stockin'&&<StockIn storeId={storeId}/>} 
 {!isCrew&&tab==='warehouse'&&<Warehouse storeId={storeId} role={profile?.role}/>} 
 {!isCrew&&tab==='revise'&&canRevise(profile?.role)&&<ReviseWarehouseStock storeId={storeId}/>}
 {!isCrew&&tab==='revise-operational'&&canRevise(profile?.role)&&<ReviseOperationalStock storeId={storeId}/>}
 {!isCrew&&tab==='revise-daily'&&canRevise(profile?.role)&&<ReviseDailyStock storeId={storeId}/>}
 {!isCrew&&tab==='revise-transfer'&&canRevise(profile?.role)&&<ReviseTransfer storeId={storeId}/>} 
 {!isCrew&&tab==='transfer'&&<Transfer storeId={storeId}/>} 
 {!isCrew&&tab==='opname'&&<Opname storeId={storeId}/>} 
 {!isCrew&&tab==='history'&&<History storeId={storeId}/>}
 {!isCrew&&tab==='products'&&profile?.role==='admin'&&<Products/>}
 {!isCrew&&tab==='users'&&profile?.role==='admin'&&<Users/>}
 </section></main></div>
}
function Login({email,password,setEmail,setPassword,login,busy,msg}){return <main className="login"><div className="brand"><div className="logo">RCM</div><h1>Management Stock</h1><p>LC Rancamanyar</p><form onSubmit={login} className="card"><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label><button disabled={busy}>{busy?'Memproses…':'Masuk'}</button>{msg&&<div className="error">{msg}</div>}</form></div></main>}

function useStock(storeId){
 const [rows,setRows]=useState([]),[products,setProducts]=useState([]),[snapshots,setSnapshots]=useState([]),[loading,setLoading]=useState(true),[error,setError]=useState('');
 async function load(){
  if(!storeId)return; setLoading(true); setError('');
  const today=new Date().toISOString().slice(0,10);
  const [{data:b,error:be},{data:p,error:pe},{data:s,error:se}]=await Promise.all([
   supabase.from('stock_balances').select('*').eq('store_id',storeId),
   supabase.from('products').select('id,code,name,category,unit,base_unit,kasir_unit,kasir_per_base,kitchen_unit,kitchen_per_base,min_stock,max_stock').eq('active',true).order('name'),
   supabase.from('stock_daily_snapshots').select('product_id,snapshot_date,opening_qty,usage_qty,ending_qty').eq('store_id',storeId).lte('snapshot_date',today).order('snapshot_date',{ascending:false}).limit(5000)
  ]);
  const firstErr=be||pe; if(firstErr)setError(firstErr.message); if(se&&!firstErr)console.warn('Snapshot tidak dapat dibaca:',se.message);
  setRows(b||[]);setProducts(p||[]);setSnapshots(s||[]);setLoading(false);
 }
 useEffect(()=>{load()},[storeId]);
 useEffect(()=>{if(!storeId)return;const channel=supabase.channel('rcm-stock-'+storeId).on('postgres_changes',{event:'*',schema:'public',table:'stock_transactions',filter:'store_id=eq.'+storeId},load).subscribe();return()=>supabase.removeChannel(channel)},[storeId]);
 const merged=useMemo(()=>products.map(p=>{
  const b=rows.find(x=>x.product_id===p.id)||{}; const ending=Number(b.gudang_qty||0)+Number(b.operasional_qty||0);
  const ps=snapshots.filter(x=>x.product_id===p.id).sort((a,b)=>String(b.snapshot_date).localeCompare(String(a.snapshot_date)));
  const todaySnap=ps.find(x=>String(x.snapshot_date)===today);
  const prev=ps.find(x=>String(x.snapshot_date)<today);
  const opening=Number(todaySnap?.opening_qty ?? prev?.ending_qty ?? ending);
  const usage=Number(todaySnap?.usage_qty ?? (opening-ending));
  return {...p,gudang_qty:Number(b.gudang_qty||0),operasional_qty:Number(b.operasional_qty||0),kasir_qty:Number(b.kasir_qty||0),kitchen_qty:Number(b.kitchen_qty||0),opening_qty:opening,usage_qty:usage,ending_qty:ending,stock_awal:opening,stock_akhir:ending,pemakaian:usage};
 }),[products,rows,snapshots]);
 return {items:merged,loading,error,reload:load};
}

function Dashboard({storeId,setTab}){
 const {items,loading,error}=useStock(storeId);
 const [summary,setSummary]=useState({transfer_transactions:0,waste_transactions:0,waste_qty:0,adjustment_qty:0});
 const [recent,setRecent]=useState([]);
 useEffect(()=>{if(!storeId)return; let live=true;
  async function load(){
   const [{data:s},{data:t}]=await Promise.all([
    supabase.from('leader_transfer_summary').select('*').eq('store_id',storeId).maybeSingle(),
    supabase.from('stock_transactions').select('id,transaction_type,qty,note,created_at,products(name,unit)').eq('store_id',storeId).order('created_at',{ascending:false}).limit(6)
   ]);
   if(live){setSummary(s||{});setRecent(t||[])}
  }
  load();
  const ch=supabase.channel('rcm-dashboard-'+storeId).on('postgres_changes',{event:'*',schema:'public',table:'stock_transactions',filter:'store_id=eq.'+storeId},load).subscribe();
  return()=>{live=false;supabase.removeChannel(ch)};
 },[storeId]);
 const low=items.filter(x=>x.ending_qty<=Number(x.min_stock||0));
 const totalG=items.reduce((s,x)=>s+x.gudang_qty,0); const totalO=items.reduce((s,x)=>s+x.operasional_qty,0);
 const totalItems=items.length;
 return <div className="leader-dashboard">
  <div className="dash-head"><div><span className="eyebrow">DASHBOARD LEADER</span><h1>Kontrol operasional hari ini</h1><p>Ringkasan stok LC Rancamanyar dalam satu layar.</p></div><button className="refresh" onClick={()=>location.reload()}>↻ Refresh</button></div>
  {error&&<div className="error">{error}</div>}
  <div className="kpi-grid">
   <Kpi icon="▣" label="Stok Gudang" value={loading?'…':fmt(totalG)} meta="Total saldo gudang"/>
   <Kpi icon="▤" label="Stok Operasional" value={loading?'…':fmt(totalO)} meta="Total saldo operasional"/>
   <Kpi icon="!" label="Stok Menipis" value={loading?'…':low.length} meta={low.length?'Perlu segera dicek':'Semua aman' } danger={low.length>0}/>
   <Kpi icon="↗" label="Transfer" value={fmt(summary.transfer_transactions)} meta="Transaksi transfer"/>
  </div>
  <div className="dash-grid">
   <div className="panel critical"><div className="panel-head"><div><h3>⚠ Produk perlu perhatian</h3><span>{low.length} produk di bawah / sama dengan minimum</span></div><button className="linkbtn" onClick={()=>setTab('warehouse')}>Lihat semua</button></div>
    {low.length===0?<div className="empty">Tidak ada produk kritis. 👍</div>:<div className="critical-list">{low.slice(0,6).map(x=><div className="critical-row" key={x.id}><div><b>{x.name}</b><small>{x.code} · Min {fmt(x.min_stock)} {x.unit}</small></div><strong>{fmt(x.operasional_qty)} {x.unit}</strong></div>)}</div>}
   </div>
   <div className="panel"><div className="panel-head"><div><h3>Ringkasan aktivitas</h3><span>Pergerakan stok tercatat</span></div></div><div className="activity-stats"><div><span>Waste</span><b>{fmt(summary.waste_qty)}</b><small>{fmt(summary.waste_transactions)} transaksi</small></div><div><span>Adjustment</span><b>{fmt(summary.adjustment_qty)}</b><small>Selisih stok</small></div><div><span>Produk Aktif</span><b>{totalItems}</b><small>Master produk</small></div></div></div>
  </div>
  <div className="panel recent"><div className="panel-head"><div><h3>Aktivitas terbaru</h3><span>Transaksi terakhir yang masuk</span></div><button className="linkbtn" onClick={()=>setTab('history')}>Riwayat →</button></div>
   {recent.length===0?<div className="empty">Belum ada transaksi.</div>:<div className="recent-list">{recent.map(r=><div className="recent-row" key={r.id}><div className={'activity-icon '+(Number(r.qty)>=0?'in':'out')}>{Number(r.qty)>=0?'↑':'↓'}</div><div><b>{r.products?.name||'Produk'}</b><small>{r.transaction_type.replaceAll('_',' ')} · {r.note||'Tanpa catatan'}</small></div><strong className={Number(r.qty)>=0?'positive':'negative'}>{Number(r.qty)>0?'+':''}{fmt(r.qty)}</strong><time>{new Date(r.created_at).toLocaleString('id-ID',{day:'2-digit',month:'short',hour:'2-digit',minute:'2-digit'})}</time></div>)}</div>}
  </div>
  <div className="quick-actions"><button onClick={()=>setTab('stockin')}>＋ Input Stok Gudang</button><button onClick={()=>setTab('transfer')}>＋ Transfer Stok</button><button onClick={()=>setTab('opname')} className="secondary">＋ Input Sisa</button><button onClick={()=>setTab('warehouse')} className="secondary">▣ Cek Stok</button></div>
 </div>
}
function Kpi({icon,label,value,meta,danger}){return <div className={'kpi '+(danger?'danger':'')}><div className="kpi-icon">{icon}</div><div><span>{label}</span><b>{value}</b><small>{meta}</small></div></div>}
function Stat({title,value}){return <div className="stat"><span>{title}</span><b>{value}</b></div>}
function Action({title,text,onClick}){return <button className="tile action" onClick={onClick}><b>{title}</b><span>{text}</span><em>Buka →</em></button>}

function StockIn({storeId}){const {items,loading,reload}=useStock(storeId);const [productId,setProductId]=useState(''),[qty,setQty]=useState(''),[note,setNote]=useState(''),[saving,setSaving]=useState(false),[msg,setMsg]=useState('');const selected=items.find(x=>x.id===productId);async function submit(e){e.preventDefault();setSaving(true);setMsg('');const amount=Number(qty);if(!Number.isFinite(amount)||amount<=0){setMsg('Jumlah stok harus lebih dari 0.');setSaving(false);return}const {error}=await supabase.rpc('record_stock_in',{p_store_id:storeId,p_product_id:productId,p_qty:amount,p_note:note||null});if(error)setMsg(error.message);else{setMsg('Stok gudang berhasil ditambahkan.');setQty('');setNote('');await reload()}setSaving(false)}return <Page title="Input Stok Gudang" subtitle="Tambah stok masuk ke gudang"><form className="card form" onSubmit={submit}><label>Produk<select value={productId} onChange={e=>setProductId(e.target.value)} required><option value="">Pilih produk</option>{items.map(x=><option key={x.id} value={x.id}>{x.name} — gudang {fmt(x.gudang_qty)} {baseUnit(x)}</option>)}</select></label>{selected&&<div className="hint">Stok gudang saat ini: <b>{fmt(selected.gudang_qty)} {baseUnit(selected)}</b></div>}<label>Jumlah stok masuk<input type="number" min="0.001" step="0.001" value={qty} onChange={e=>setQty(e.target.value)} required placeholder="Contoh: 10"/></label><label>Catatan<input value={note} onChange={e=>setNote(e.target.value)} placeholder="Contoh: Penerimaan supplier"/></label><button disabled={saving||loading}>{saving?'Menyimpan…':'＋ Tambah Stok Gudang'}</button>{msg&&<div className={msg.includes('berhasil')?'success':'error'}>{msg}</div>}</form></Page>}

function Warehouse({storeId,role}){const {items,loading,error}=useStock(storeId);return <Page title="Stok Gudang" subtitle="Stok awal, pemakaian, dan stok akhir"><div className="table-wrap">{loading?<p>Memuat stok…</p>:error?<div className="error">{error}</div>:<table><thead><tr><th>Produk</th><th>Satuan Base</th><th>Stok Awal</th><th>Pemakaian</th><th>Stok Akhir</th><th>Status</th></tr></thead><tbody>{items.map(x=><tr key={x.id}><td><b>{x.name}</b><small>{x.code}</small></td><td>{baseUnit(x)}</td><td>{fmt(x.opening_qty)} {baseUnit(x)}</td><td>{fmt(x.usage_qty)} {baseUnit(x)}</td><td><b>{fmt(x.ending_qty)} {baseUnit(x)}</b></td><td><Status item={{...x,operasional_qty:x.ending_qty}}/></td></tr>)}</tbody></table>}</div>{role&&<div className="notice">Stok Akhir = Gudang + Operasional (Kasir + Kitchen) dalam satuan Base.</div>}</Page>}

function Status({item}){if(item.operasional_qty<=Number(item.min_stock||0))return <span className="badge danger">Menipis</span>;if(item.max_stock&&item.operasional_qty>=Number(item.max_stock))return <span className="badge warn">Penuh</span>;return <span className="badge ok">Normal</span>}

function ReviseWarehouseStock({storeId}){const {items,loading,reload}=useStock(storeId);const [productId,setProductId]=useState(''),[qty,setQty]=useState(''),[unit,setUnit]=useState('pcs'),[note,setNote]=useState(''),[saving,setSaving]=useState(false),[msg,setMsg]=useState('');const selected=items.find(x=>x.id===productId);useEffect(()=>{if(selected){setQty(String(selected.gudang_qty||0));setUnit(selected.unit||'pcs');}},[productId]);async function submit(e){e.preventDefault();setSaving(true);setMsg('');try{const amount=Number(qty);if(!Number.isFinite(amount)||amount<0)throw new Error('Stok baru harus 0 atau lebih.');if(!selected)throw new Error('Pilih produk terlebih dahulu.');if(Math.abs(amount-Number(selected.gudang_qty||0))<0.0000001&&unit===selected.unit)throw new Error('Tidak ada perubahan stok atau satuan.');if(!note.trim())throw new Error('Catatan revisi wajib diisi agar perubahan dapat ditelusuri.');const {error}=await supabase.rpc('record_warehouse_stock_revision',{p_store_id:storeId,p_product_id:productId,p_new_qty:amount,p_note:note.trim(),p_unit:unit});if(error)throw error;setMsg('Revisi stok gudang dan satuan berhasil disimpan.');setNote('');await reload()}catch(err){setMsg(err?.message||'Gagal menyimpan revisi stok gudang.')}finally{setSaving(false)}}return <Page title="Revisi Stok Gudang" subtitle="Rubah stok gudang dengan catatan audit"><div className="notice">Menu ini hanya dapat diakses Admin, Store Leader, dan Team Leader. Setiap perubahan otomatis tercatat sebagai adjustment di Riwayat.</div><form className="card form" onSubmit={submit}><label>Produk<select value={productId} onChange={e=>setProductId(e.target.value)} required><option value="">Pilih produk</option>{items.map(x=><option key={x.id} value={x.id}>{x.name} — stok gudang {fmt(x.gudang_qty)} {x.unit}</option>)}</select></label>{selected&&<div className="hint">Stok saat ini: <b>{fmt(selected.gudang_qty)} {baseUnit(selected)}</b></div>}<label>Satuan<select value={unit} onChange={e=>setUnit(e.target.value)} required><option value="gr">gr</option><option value="kg">kg</option><option value="pcs">pcs</option></select></label><label>Stok gudang setelah revisi<input type="number" min="0" step="0.001" value={qty} onChange={e=>setQty(e.target.value)} required/></label><label>Alasan / Catatan Revisi<input value={note} onChange={e=>setNote(e.target.value)} placeholder="Contoh: Koreksi hasil pengecekan fisik" required/></label><button disabled={saving||loading}>{saving?'Menyimpan…':'✓ Simpan Revisi Stok'}</button>{msg&&<div className={msg.includes('berhasil')?'success':'error'}>{msg}</div>}</form></Page>}

function Transfer({storeId}){
 const {items,loading,reload}=useStock(storeId);const [productId,setProductId]=useState(''),[operationalArea,setOperationalArea]=useState('kitchen'),[qty,setQty]=useState(''),[note,setNote]=useState(''),[saving,setSaving]=useState(false),[msg,setMsg]=useState('');
 const selected=items.find(x=>x.id===productId);const amount=Number(qty||0);const cfg=areaConfig(selected,operationalArea);const maxBase=selected?Number(selected.gudang_qty||0):0;const invalid=!!selected&&(!Number.isFinite(amount)||amount<=0||amount>maxBase+1e-9);
 async function submit(e){e.preventDefault();setMsg('');if(!selected)return setMsg('Pilih produk terlebih dahulu.');if(!Number.isFinite(amount)||amount<=0)return setMsg('Jumlah transfer harus lebih dari 0.');if(amount>maxBase+1e-9)return setMsg('Jumlah transfer melebihi stok gudang.');setSaving(true);try{const {error}=await supabase.rpc('record_stock_transfer_v2',{p_store_id:storeId,p_product_id:productId,p_area_qty:baseToArea(amount,selected,operationalArea),p_note:note.trim()||null,p_operational_area:operationalArea});if(error)throw error;setMsg('Transfer berhasil. Stok gudang dan operasional sudah diperbarui.');setQty('');setNote('');await reload()}catch(err){setMsg(err?.message||'Transfer gagal disimpan.')}finally{setSaving(false)}}
 return <Page title="Transfer Stok" subtitle="Gudang → Operasional"><div className="notice">Jumlah transfer selalu diinput menggunakan <b>satuan Base</b>. Sistem otomatis mengonversinya ke satuan Kasir/Kitchen sesuai Master Produk. Kasir + Kitchen tetap digabung saat menampilkan stok operasional.</div><form className="card form" onSubmit={submit}><label>Tujuan Operasional<select value={operationalArea} onChange={e=>{setOperationalArea(e.target.value);setQty('');setMsg('')}} required><option value="kasir">Kasir</option><option value="kitchen">Kitchen</option></select></label><label>Produk<select value={productId} onChange={e=>{setProductId(e.target.value);setQty('');setMsg('')}} required><option value="">Pilih produk</option>{items.map(x=><option key={x.id} value={x.id}>{x.name} — gudang {fmt(x.gudang_qty)} {baseUnit(x)}</option>)}</select></label>{selected&&<div className="stock-preview"><div><small>STOK GUDANG</small><b>{fmt(selected.gudang_qty)} {baseUnit(selected)}</b></div><div>→</div><div><small>TOTAL OPERASIONAL</small><b>{fmt(selected.operasional_qty)} {baseUnit(selected)}</b><small>Tujuan: {operationalArea==='kasir'?'Kasir':'Kitchen'} · {cfg.unit}</small></div></div>}<label>Jumlah Transfer ({baseUnit(selected)})<input type="number" min="0.000001" max={maxBase||undefined} step="0.000001" value={qty} onChange={e=>setQty(e.target.value)} required/></label>{selected&&<div className={invalid?'error':'hint'}>{invalid?'Jumlah tidak boleh melebihi stok gudang.':<>Maksimal: <b>{fmt(maxBase)} {baseUnit(selected)}</b> · Sistem akan mengirim <b>{fmt(baseToArea(amount,selected,operationalArea))} {cfg.unit}</b> ke {operationalArea==='kasir'?'Kasir':'Kitchen'}.</>}</div>}<label>Catatan<input value={note} onChange={e=>setNote(e.target.value)} placeholder="Contoh: Pengambilan stok untuk operasional"/></label><button disabled={saving||loading||invalid}>{saving?'Menyimpan…':'Transfer Stok'}</button>{msg&&<div className={msg.includes('berhasil')?'success':'error'}>{msg}</div>}</form></Page>
}

function Opname({storeId}){
 const {items,loading,reload}=useStock(storeId); const [productId,setProductId]=useState(''),[operationalArea,setOperationalArea]=useState('kitchen'),[areaSystem,setAreaSystem]=useState(null),[physical,setPhysical]=useState(''),[waste,setWaste]=useState('0'),[note,setNote]=useState(''),[saving,setSaving]=useState(false),[result,setResult]=useState(null),[msg,setMsg]=useState('');
 const selected=items.find(x=>x.id===productId);
 useEffect(()=>{let live=true;async function loadArea(){if(!storeId||!productId){setAreaSystem(null);return}const {data,error}=await supabase.from('operational_area_balances').select('qty').eq('store_id',storeId).eq('product_id',productId).eq('operational_area',operationalArea).maybeSingle();if(live)setAreaSystem(error?null:Number(data?.qty||0))}loadArea();return()=>{live=false}},[storeId,productId,operationalArea]);
 async function submit(e){e.preventDefault();setSaving(true);setMsg('');setResult(null);const {data,error}=await supabase.rpc('record_operational_opname',{p_store_id:storeId,p_product_id:productId,p_physical_stock:Number(physical),p_waste_qty:Number(waste||0),p_note:note||null,p_operational_area:operationalArea});if(error)setMsg(error.message);else{setResult(data);setPhysical('');setWaste('0');setNote('');await reload()}setSaving(false)}
 return <Page title="Input Sisa" subtitle="Opname stok operasional"><form className="card form" onSubmit={submit}><label>Area Operasional<select value={operationalArea} onChange={e=>setOperationalArea(e.target.value)} required><option value="kasir">Kasir</option><option value="kitchen">Kitchen</option></select></label><label>Produk<select value={productId} onChange={e=>setProductId(e.target.value)} required><option value="">Pilih produk</option>{items.map(x=><option key={x.id} value={x.id}>{x.name} — total operasional {fmt(x.operasional_qty)} {x.unit}</option>)}</select></label>{selected&&<div className="hint">Sistem {operationalArea==='kasir'?'Kasir':'Kitchen'}: <b>{fmt(areaSystem)} {selected.unit}</b> · Total operasional: <b>{fmt(selected.operasional_qty)} {baseUnit(selected)}</b></div>}<label>Sisa fisik<input type="number" min="0" step="0.001" value={physical} onChange={e=>setPhysical(e.target.value)} required/></label><label>Waste<input type="number" min="0" step="0.001" value={waste} onChange={e=>setWaste(e.target.value)}/></label><label>Catatan<input value={note} onChange={e=>setNote(e.target.value)} placeholder="Opsional"/></label><button disabled={saving||loading}>{saving?'Menyimpan…':'Simpan Opname'}</button>{msg&&<div className="error">{msg}</div>}{result&&<div className="result"><b>Opname tersimpan</b><div>Area: {result.operational_area==='kasir'?'Kasir':'Kitchen'}</div><div>Pemakaian: {fmt(result.usage_qty)}</div><div>Waste: {fmt(result.waste_qty)}</div><div>Surplus: {fmt(result.surplus_qty)}</div></div>}</form></Page>}

function History({storeId}){
 const [rows,setRows]=useState([]),[loading,setLoading]=useState(true),[error,setError]=useState('');
 async function load(){if(!storeId)return;setLoading(true);const {data,error}=await supabase.from('stock_transactions').select('id,product_id,area,transaction_type,qty,note,created_at,created_by,operational_area,reference_id,transaction_no,products(name,unit)').eq('store_id',storeId).order('created_at',{ascending:false}).limit(100);if(error)setError(error.message);setRows(data||[]);setLoading(false)}
 useEffect(()=>{load()},[storeId]);useEffect(()=>{if(!storeId)return;const ch=supabase.channel('rcm-history-'+storeId).on('postgres_changes',{event:'*',schema:'public',table:'stock_transactions',filter:'store_id=eq.'+storeId},load).subscribe();return()=>supabase.removeChannel(ch)},[storeId]);
 return <Page title="Riwayat" subtitle="100 transaksi terbaru"><div className="history-tools"><button className="refresh" onClick={load}>↻ Refresh</button></div><div className="history">{loading?<p>Memuat riwayat…</p>:error?<div className="error">{error}</div>:rows.length===0?<p>Belum ada transaksi.</p>:rows.map(r=><div className="history-row" key={r.id}><div><b>{r.products?.name||'Produk'}</b><small>{friendlyType(r.transaction_type)} · {r.area==='operasional'?(r.operational_area?`Operasional ${r.operational_area}`:'Operasional'):'Gudang'}{r.transaction_no?` · ${r.transaction_no}`:''}</small><small>{r.note||'Tanpa catatan'}</small></div><strong className={Number(r.qty)<0?'negative':'positive'}>{Number(r.qty)>0?'+':''}{fmt(r.qty)} {r.products?.unit||''}</strong><time>{new Date(r.created_at).toLocaleString('id-ID',{day:'2-digit',month:'short',hour:'2-digit',minute:'2-digit'})}</time></div>)}</div></Page>}

function ReviseOperationalStock({storeId}){
 const {items,loading,reload}=useStock(storeId);const [productId,setProductId]=useState(''),[area,setArea]=useState('kitchen'),[qty,setQty]=useState(''),[note,setNote]=useState(''),[saving,setSaving]=useState(false),[msg,setMsg]=useState('');const selected=items.find(x=>x.id===productId);
 useEffect(()=>{if(selected)setQty('')},[productId,area]);
 async function submit(e){e.preventDefault();setMsg('');if(!selected)return setMsg('Pilih produk terlebih dahulu.');const n=Number(qty);if(!Number.isFinite(n)||n<0)return setMsg('Stok baru harus 0 atau lebih.');if(!note.trim())return setMsg('Alasan revisi wajib diisi.');setSaving(true);try{const {data,error}=await supabase.rpc('record_operational_stock_revision',{p_store_id:storeId,p_product_id:productId,p_target:'operational',p_new_qty:n,p_note:note.trim(),p_operational_area:area});if(error)throw error;setMsg(`Revisi berhasil${data?.transaction_no?` · ${data.transaction_no}`:''}`);setQty('');setNote('');await reload()}catch(err){setMsg(err.message||'Revisi gagal.')}finally{setSaving(false)}}
 return <Page title="Revisi Stok Operasional" subtitle="Koreksi stok Kasir/Kitchen dengan audit"><div className="notice">Khusus Admin, Store Leader, dan Team Leader. Nilai yang dimasukkan mengikuti satuan area produk; sistem mengonversinya ke satuan dasar dan tetap menjaga total Kasir + Kitchen.</div><form className="card form" onSubmit={submit}><label>Area<select value={area} onChange={e=>setArea(e.target.value)}><option value="kasir">Kasir</option><option value="kitchen">Kitchen</option></select></label><label>Produk<select value={productId} onChange={e=>setProductId(e.target.value)} required><option value="">Pilih produk</option>{items.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select></label>{selected&&<div className="hint">Total operasional saat ini: <b>{fmt(selected.operasional_qty)} {selected.base_unit||selected.unit}</b></div>}<label>Stok {area==='kasir'?'Kasir':'Kitchen'} setelah revisi<input type="number" min="0" step="0.001" value={qty} onChange={e=>setQty(e.target.value)} required/></label><label>Alasan / Catatan<input value={note} onChange={e=>setNote(e.target.value)} required placeholder="Contoh: Koreksi hasil hitung fisik"/></label><button disabled={saving||loading}>{saving?'Menyimpan…':'✓ Simpan Revisi Operasional'}</button>{msg&&<div className={msg.includes('berhasil')?'success':'error'}>{msg}</div>}</form></Page>}

function ReviseTransfer({storeId}){
 const [rows,setRows]=useState([]),[loading,setLoading]=useState(true),[saving,setSaving]=useState(false),[error,setError]=useState(''),[selected,setSelected]=useState(null),[qty,setQty]=useState(''),[note,setNote]=useState('');
 async function load(){setLoading(true);const {data,error}=await supabase.from('stock_transactions').select('id,product_id,qty,created_at,reference_id,transaction_no,note,operational_area,products(name,unit)').eq('store_id',storeId).eq('transaction_type','transfer_out').order('created_at',{ascending:false}).limit(50);if(error)setError(error.message);setRows(data||[]);setLoading(false)}
 useEffect(()=>{load()},[storeId]);
 function choose(r){setSelected(r);setQty(String(Math.abs(Number(r.qty||0))));setNote('')}
 async function submit(e){e.preventDefault();if(!selected)return setError('Pilih transaksi transfer terlebih dahulu.');const n=Number(qty);if(!Number.isFinite(n)||n<=0)return setError('Jumlah transfer baru harus lebih dari 0.');if(!note.trim())return setError('Alasan revisi wajib diisi.');setSaving(true);setError('');try{const {data,error}=await supabase.rpc('record_stock_transfer_revision',{p_store_id:storeId,p_reference_id:selected.reference_id,p_new_qty:n,p_note:note.trim()});if(error)throw error;setError(`Revisi transfer berhasil${data?.transaction_no?` · ${data.transaction_no}`:''}`);setSelected(null);setQty('');setNote('');await load()}catch(err){setError(err.message||'Revisi transfer gagal.')}finally{setSaving(false)}}
 return <Page title="Revisi Transfer" subtitle="Koreksi jumlah transfer tanpa menghapus riwayat"><div className="notice">Revisi dibuat sebagai adjustment. Transaksi transfer asli tetap tersimpan sehingga audit tetap aman.</div>{error&&<div className={error.includes('berhasil')?'success':'error'}>{error}</div>}<div className="table-wrap">{loading?<p>Memuat transfer…</p>:rows.length===0?<p>Belum ada transfer.</p>:<table><thead><tr><th>No. Transaksi</th><th>Produk</th><th>Tujuan</th><th>Jumlah</th><th>Waktu</th><th></th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{r.transaction_no||r.reference_id?.slice(0,8)}</td><td><b>{r.products?.name}</b><small>{r.products?.unit}</small></td><td>{r.operational_area||'-'}</td><td>{fmt(Math.abs(r.qty))}</td><td>{new Date(r.created_at).toLocaleString('id-ID',{day:'2-digit',month:'short',hour:'2-digit',minute:'2-digit'})}</td><td><button className="smallbtn" onClick={()=>choose(r)}>Revisi</button></td></tr>)}</tbody></table>}</div>{selected&&<form className="card form" onSubmit={submit}><h3>Revisi Transfer</h3><div className="hint"><b>{selected.products?.name}</b> · {selected.operational_area||'-'} · sebelumnya {fmt(Math.abs(selected.qty))} {selected.products?.unit}</div><label>Jumlah Transfer Baru<input type="number" min="0.001" step="0.001" value={qty} onChange={e=>setQty(e.target.value)} required/></label><label>Alasan / Catatan<input value={note} onChange={e=>setNote(e.target.value)} required placeholder="Contoh: Salah input 10 menjadi 8"/></label><div className="actions"><button disabled={saving}>{saving?'Menyimpan…':'✓ Simpan Revisi Transfer'}</button><button type="button" className="secondary" onClick={()=>setSelected(null)}>Batal</button></div></form>}</Page>}

function ReviseDailyStock({storeId}){
 const {items,loading,reload}=useStock(storeId);const [productId,setProductId]=useState(''),[target,setTarget]=useState('opening'),[area,setArea]=useState('kitchen'),[qty,setQty]=useState(''),[note,setNote]=useState(''),[saving,setSaving]=useState(false),[msg,setMsg]=useState('');const selected=items.find(x=>x.id===productId);
 async function submit(e){e.preventDefault();if(!selected)return setMsg('Pilih produk terlebih dahulu.');const n=Number(qty);if(!Number.isFinite(n)||n<0)return setMsg('Nilai stok harus 0 atau lebih.');if(!note.trim())return setMsg('Alasan revisi wajib diisi.');setSaving(true);setMsg('');try{const {data,error}=await supabase.rpc('record_operational_stock_revision',{p_store_id:storeId,p_product_id:productId,p_target:target,p_new_qty:n,p_note:note.trim(),p_operational_area:target==='ending'?area:null});if(error)throw error;setMsg(`Revisi ${target==='opening'?'stok awal':'stok akhir'} berhasil${data?.transaction_no?` · ${data.transaction_no}`:''}`);setQty('');setNote('');await reload()}catch(err){setMsg(err.message||'Revisi gagal.')}finally{setSaving(false)}}
 return <Page title="Revisi Stok Awal / Akhir" subtitle="Koreksi angka laporan harian tanpa menghapus transaksi"><form className="card form" onSubmit={submit}><label>Jenis Revisi<select value={target} onChange={e=>setTarget(e.target.value)}><option value="opening">Stok Awal</option><option value="ending">Stok Akhir</option></select></label>{target==='ending'&&<label>Area Penyesuaian<select value={area} onChange={e=>setArea(e.target.value)}><option value="kasir">Kasir</option><option value="kitchen">Kitchen</option></select></label>}<label>Produk<select value={productId} onChange={e=>setProductId(e.target.value)} required><option value="">Pilih produk</option>{items.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select></label>{selected&&<div className="hint">Saldo sekarang: <b>{fmt(selected.gudang_qty+selected.operasional_qty)} {selected.base_unit||selected.unit}</b></div>}<label>Nilai baru<input type="number" min="0" step="0.001" value={qty} onChange={e=>setQty(e.target.value)} required/></label><label>Alasan / Catatan<input value={note} onChange={e=>setNote(e.target.value)} required/></label><button disabled={saving||loading}>{saving?'Menyimpan…':'✓ Simpan Revisi'}</button>{msg&&<div className={msg.includes('berhasil')?'success':'error'}>{msg}</div>}</form></Page>}

function CrewStock({storeId}){const {items,loading,error}=useStock(storeId);return <Page title="Stok" subtitle="Stok awal, pemakaian, dan stok akhir">{error&&<div className="error">{error}</div>}<div className="table-wrap">{loading?<p>Memuat stok…</p>:items.length===0?<div className="empty">Belum ada produk aktif.</div>:<table><thead><tr><th>Produk</th><th>Satuan Base</th><th>Stok Awal</th><th>Pemakaian</th><th>Stok Akhir</th><th>Status</th></tr></thead><tbody>{items.map(x=><tr key={x.id}><td><b>{x.name}</b><small>{x.code}</small></td><td>{baseUnit(x)}</td><td>{fmt(x.opening_qty)} {baseUnit(x)}</td><td>{fmt(x.usage_qty)} {baseUnit(x)}</td><td><b>{fmt(x.ending_qty)} {baseUnit(x)}</b></td><td><Status item={{...x,operasional_qty:x.ending_qty}}/></td></tr>)}</tbody></table>}</div><div className="notice">Stok operasional merupakan gabungan Kasir + Kitchen dan ditampilkan dalam satuan Base.</div></Page>}

function CrewOpname({storeId}){
 const {items,loading,reload}=useStock(storeId); const [productId,setProductId]=useState(''),[operationalArea,setOperationalArea]=useState('kitchen'),[areaSystem,setAreaSystem]=useState(null),[physical,setPhysical]=useState(''),[waste,setWaste]=useState('0'),[saving,setSaving]=useState(false),[msg,setMsg]=useState('');
 const selected=items.find(x=>x.id===productId);
 useEffect(()=>{let live=true;async function loadArea(){if(!storeId||!productId){setAreaSystem(null);return}const {data}=await supabase.from('operational_area_balances').select('qty').eq('store_id',storeId).eq('product_id',productId).eq('operational_area',operationalArea).maybeSingle();if(live)setAreaSystem(Number(data?.qty||0))}loadArea();return()=>{live=false}},[storeId,productId,operationalArea]);
 async function submit(e){e.preventDefault();setMsg('');if(!selected)return setMsg('Pilih produk terlebih dahulu.');const qty=Number(physical),w=Number(waste||0);if(!Number.isFinite(qty)||qty<0)return setMsg('Sisa fisik tidak boleh kurang dari 0.');if(!Number.isFinite(w)||w<0)return setMsg('Waste tidak boleh kurang dari 0.');setSaving(true);const {error}=await supabase.rpc('record_operational_opname',{p_store_id:storeId,p_product_id:productId,p_physical_stock:qty,p_waste_qty:w,p_note:null,p_operational_area:operationalArea});if(error)setMsg(error.message);else{setMsg('Sisa stok berhasil disimpan.');setPhysical('');setWaste('0');await reload()}setSaving(false)}
 return <Page title="Input Sisa" subtitle="Masukkan sisa stok yang kamu hitung"><div className="crew-tip">💡 Pilih area yang dihitung. Total stok operasional di aplikasi tetap merupakan gabungan Kasir + Kitchen.</div><form className="card form crew-form" onSubmit={submit}><label>1. Pilih Area<select value={operationalArea} onChange={e=>setOperationalArea(e.target.value)} required><option value="kasir">Kasir</option><option value="kitchen">Kitchen</option></select></label><label>2. Pilih Produk<select value={productId} onChange={e=>setProductId(e.target.value)} required><option value="">Pilih produk</option>{items.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select></label>{selected&&<div className="crew-current"><span>Sistem {operationalArea==='kasir'?'Kasir':'Kitchen'} · Total operasional</span><b>{fmt(areaSystem)} · {fmt(selected.operasional_qty)} {baseUnit(selected)}</b></div>}<label>3. Sisa Fisik<input className="crew-big-input" type="number" min="0" step="0.001" value={physical} onChange={e=>setPhysical(e.target.value)} required placeholder="Masukkan jumlah sisa"/></label><details className="crew-optional"><summary>Tambah waste (opsional)</summary><label>Waste<input type="number" min="0" step="0.001" value={waste} onChange={e=>setWaste(e.target.value)}/></label></details><button className="crew-save" disabled={saving||loading}>{saving?'Menyimpan…':'✓ Simpan Sisa Stok'}</button>{msg&&<div className={msg.includes('berhasil')?'success':'error'}>{msg}</div>}</form></Page>
}

function MyHistory({storeId}){const [rows,setRows]=useState([]),[loading,setLoading]=useState(true),[error,setError]=useState('');async function load(){if(!storeId)return;setLoading(true);const {data:user}=await supabase.auth.getUser();if(!user?.user){setLoading(false);return}const {data,error}=await supabase.from('stock_transactions').select('id,area,transaction_type,qty,note,created_at,products(name,unit)').eq('store_id',storeId).eq('created_by',user.user.id).order('created_at',{ascending:false}).limit(50);if(error)setError(error.message);setRows(data||[]);setLoading(false)}useEffect(()=>{load()},[storeId]);useEffect(()=>{if(!storeId)return;const channel=supabase.channel('rcm-my-history-'+storeId).on('postgres_changes',{event:'INSERT',schema:'public',table:'stock_transactions',filter:'store_id=eq.'+storeId},load).subscribe();return()=>supabase.removeChannel(channel)},[storeId]);return <Page title="Riwayat Saya" subtitle="Aktivitas stok yang kamu input"><div className="history">{loading?<p className="empty">Memuat riwayat…</p>:error?<div className="error">{error}</div>:rows.length===0?<p className="empty">Belum ada aktivitas stok dari akun ini.</p>:rows.map(r=><div className="history-row" key={r.id}><div><b>{r.products?.name||'Produk'}</b><small>{friendlyType(r.transaction_type)} · {r.area==='operasional'?'Operasional':'Gudang'}</small></div><strong className={Number(r.qty)<0?'negative':'positive'}>{Number(r.qty)>0?'+':''}{fmt(r.qty)} {r.products?.unit||''}</strong><time>{new Date(r.created_at).toLocaleString('id-ID',{day:'2-digit',month:'short',hour:'2-digit',minute:'2-digit'})}</time></div>)}</div></Page>}
function friendlyType(t){return ({stock_in:'Stok masuk',transfer_in:'Stok masuk operasional',transfer_out:'Transfer keluar',adjustment:'Penyesuaian',waste:'Waste'})[t]||t.replaceAll('_',' ')}

function Page({title,subtitle,children}){return <div className="page"><div className="page-title"><div><h1>{title}</h1><p>{subtitle}</p></div></div>{children}</div>}

function Products(){
 const [rows,setRows]=useState([]),[loading,setLoading]=useState(true),[saving,setSaving]=useState(false),[error,setError]=useState('');
 const blank={id:null,code:'',name:'',category:'',unit:'pcs',base_unit:'pcs',kasir_unit:'pcs',kasir_per_base:1,kitchen_unit:'pcs',kitchen_per_base:1,min_stock:0,max_stock:0,active:true}; const [form,setForm]=useState(blank);
 async function load(){setLoading(true);setError('');const {data,error}=await supabase.from('products').select('*').order('name');if(error)setError(error.message);setRows(data||[]);setLoading(false)} useEffect(()=>{load()},[]);
 function edit(r){setForm({...blank,...r})} function reset(){setForm({...blank})}
 async function save(e){e.preventDefault();setSaving(true);setError('');try{const min=Number(form.min_stock||0),max=Number(form.max_stock||0),kp=Number(form.kasir_per_base||0),kip=Number(form.kitchen_per_base||0);if(min<0||max<0||max>0&&max<min)throw new Error('Min/Max Stock tidak valid.');if(kp<=0||kip<=0)throw new Error('Konversi Kasir/Kitchen harus lebih dari 0.');const payload={p_product_id:form.id,p_code:form.code.trim(),p_name:form.name.trim(),p_category:form.category?.trim()||null,p_unit:form.unit||'pcs',p_min_stock:min,p_max_stock:max,p_active:!!form.active,p_base_unit:form.base_unit||form.unit||'pcs',p_kasir_unit:form.kasir_unit||form.base_unit||form.unit||'pcs',p_kasir_per_base:kp,p_kitchen_unit:form.kitchen_unit||form.base_unit||form.unit||'pcs',p_kitchen_per_base:kip};if(!payload.p_code||!payload.p_name)throw new Error('Kode dan nama produk wajib diisi.');let result;if(form.id)result=await supabase.rpc('update_product_master',payload);else result=await supabase.from('products').insert({code:payload.p_code,name:payload.p_name,category:payload.p_category,unit:payload.p_unit,min_stock:min,max_stock:max,active:payload.p_active,base_unit:payload.p_base_unit,kasir_unit:payload.p_kasir_unit,kasir_per_base:kp,kitchen_unit:payload.p_kitchen_unit,kitchen_per_base:kip});if(result.error)throw result.error;reset();await load()}catch(err){setError(err?.message||'Gagal menyimpan produk.')}finally{setSaving(false)}}
 async function toggle(r){setSaving(true);setError('');try{const {error}=await supabase.rpc('set_product_active',{p_product_id:r.id,p_active:!r.active});if(error)throw error;await load()}catch(err){setError(err?.message||'Gagal mengubah status produk.')}finally{setSaving(false)}}
 return <Page title="Master Produk" subtitle="Kelola produk, satuan Base dan konversi Kasir/Kitchen"><div className="split"><form className="card form" onSubmit={save}><h3>{form.id?'Edit Produk':'Tambah Produk'}</h3><label>Kode Produk<input value={form.code} onChange={e=>setForm({...form,code:e.target.value})} required/></label><label>Nama Produk<input value={form.name} onChange={e=>setForm({...form,name:e.target.value})} required/></label><label>Kategori<input value={form.category||''} onChange={e=>setForm({...form,category:e.target.value})}/></label><label>Satuan Base<select value={form.base_unit} onChange={e=>setForm({...form,base_unit:e.target.value,unit:e.target.value})} required>{UNIT_OPTIONS.map(u=><option key={u} value={u}>{u}</option>)}</select></label><div className="two"><label>Satuan Kasir<select value={form.kasir_unit} onChange={e=>setForm({...form,kasir_unit:e.target.value})}>{UNIT_OPTIONS.map(u=><option key={u} value={u}>{u}</option>)}</select></label><label>Kasir: jumlah per 1 Base<input type="number" min="0.000001" step="0.000001" value={form.kasir_per_base} onChange={e=>setForm({...form,kasir_per_base:e.target.value})}/></label></div><div className="two"><label>Satuan Kitchen<select value={form.kitchen_unit} onChange={e=>setForm({...form,kitchen_unit:e.target.value})}>{UNIT_OPTIONS.map(u=><option key={u} value={u}>{u}</option>)}</select></label><label>Kitchen: jumlah per 1 Base<input type="number" min="0.000001" step="0.000001" value={form.kitchen_per_base} onChange={e=>setForm({...form,kitchen_per_base:e.target.value})}/></label></div><div className="hint">Contoh: Base kg, Kasir pcs, Kasir per Base = 12 → 12 pcs = 1 kg. Kitchen dapat memakai satuan/rasio berbeda.</div><div className="two"><label>Min Stock<input type="number" min="0" step="0.001" value={form.min_stock} onChange={e=>setForm({...form,min_stock:e.target.value})}/></label><label>Max Stock<input type="number" min="0" step="0.001" value={form.max_stock} onChange={e=>setForm({...form,max_stock:e.target.value})}/></label></div><label className="check"><input type="checkbox" checked={!!form.active} onChange={e=>setForm({...form,active:e.target.checked})}/> Produk aktif</label><div className="actions"><button disabled={saving}>{saving?'Menyimpan…':form.id?'Simpan Perubahan':'Tambah Produk'}</button>{form.id&&<button type="button" className="secondary" onClick={reset}>Batal</button>}</div>{error&&<div className="error">{error}</div>}</form><div className="table-wrap">{loading?<p>Memuat produk…</p>:rows.length===0?<p>Belum ada produk. Tambahkan produk pertama.</p>:<table><thead><tr><th>Produk</th><th>Base</th><th>Kasir</th><th>Kitchen</th><th>Min/Max</th><th>Status</th><th></th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td><b>{r.name}</b><small>{r.code}</small></td><td>{baseUnit(r)}</td><td>{r.kasir_unit||baseUnit(r)} × {fmt(r.kasir_per_base||1)}</td><td>{r.kitchen_unit||baseUnit(r)} × {fmt(r.kitchen_per_base||1)}</td><td>{fmt(r.min_stock)} / {fmt(r.max_stock)}</td><td>{r.active?<span className="badge ok">Aktif</span>:<span className="badge danger">Nonaktif</span>}</td><td><button className="smallbtn" onClick={()=>edit(r)}>Edit</button><button className="smallbtn" onClick={()=>toggle(r)} disabled={saving}>{r.active?'Nonaktifkan':'Aktifkan'}</button></td></tr>)}</tbody></table>}</div></div></Page>}

function Users(){
 const [rows,setRows]=useState([]),[stores,setStores]=useState([]),[loading,setLoading]=useState(true),[saving,setSaving]=useState(false),[error,setError]=useState('');
 async function load(){setLoading(true);const [{data:u,error:ue},{data:s,error:se}]=await Promise.all([supabase.from('profiles').select('id,full_name,role,store_id,active,created_at,stores(name,code)').order('full_name'),supabase.from('stores').select('id,name,code').eq('active',true).order('name')]);if(ue||se)setError((ue||se).message);setRows(u||[]);setStores(s||[]);setLoading(false)}
 useEffect(()=>{load()},[]);
 async function update(id,patch){setSaving(true);setError('');const {error}=await supabase.from('profiles').update(patch).eq('id',id);if(error)setError(error.message);else await load();setSaving(false)}
 return <Page title="User / Role" subtitle="Kelola role, store dan status pengguna"><div className="notice">Akun login dibuat melalui Supabase Auth. Di sini Admin mengatur profil, role, store dan status aktif pengguna.</div><div className="table-wrap">{loading?<p>Memuat pengguna…</p>:rows.length===0?<p>Belum ada profil pengguna.</p>:<table><thead><tr><th>Pengguna</th><th>Role</th><th>Store</th><th>Status</th><th>Aksi</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td><b>{r.full_name||'Tanpa Nama'}</b><small>{r.id.slice(0,8)}…</small></td><td><select value={r.role} disabled={saving} onChange={e=>update(r.id,{role:e.target.value})}><option value="admin">Admin</option><option value="store_leader">Store Leader</option><option value="team_leader">Team Leader</option><option value="crew">Crew</option></select></td><td><select value={r.store_id||''} disabled={saving} onChange={e=>update(r.id,{store_id:e.target.value||null})}><option value="">-</option>{stores.map(st=><option key={st.id} value={st.id}>{st.name}</option>)}</select></td><td>{r.active?<span className="badge ok">Aktif</span>:<span className="badge danger">Nonaktif</span>}</td><td><button className="smallbtn" disabled={saving} onClick={()=>update(r.id,{active:!r.active})}>{r.active?'Nonaktifkan':'Aktifkan'}</button></td></tr>)}</tbody></table>}{error&&<div className="error">{error}</div>}</div></Page>
}
